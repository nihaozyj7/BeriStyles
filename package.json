{
  "name": "通用样式",
  "version": "1.0.0",
  "description": "一套轻量级、支持浅色/深色双主题的现代表单样式系统",
  "main": "index.js",
  "scripts": {
    "build": "pnpm run build:css && pnpm run build:js",
    "build:css": "pnpm exec rimraf css && pnpm exec sass scss:css --no-source-map",
    "build:css:watch": "pnpm exec sass scss:css --watch",
    "build:js": "pnpm exec tsc",
    "build:js:watch": "pnpm exec tsc --watch",
    "dev": "pnpm run build && concurrently \"pnpm run build:css:watch\" \"pnpm run build:js:watch\"",
    "clean": "pnpm exec rimraf css js"
  },
  "keywords": [
    "scss",
    "css",
    "form",
    "ui",
    "theme",
    "dark-mode",
    "light-mode"
  ],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.30.3",
  "devDependencies": {
    "concurrently": "^9.0.0",
    "rimraf": "^6.1.3",
    "sass": "^1.97.3",
    "typescript": "^5.4.0"
  }
}
